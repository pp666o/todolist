# Optional semantic-retrieval and embedding dependencies.
#
# These dependencies are intentionally separated from requirements.txt
# because PyTorch and its runtime packages are substantially larger than
# the core FastAPI dependencies.
#
# restore_search_assets.sh installs this file only when the BGE runtime
# dependencies are missing.
huggingface-hub==1.24.0
sentence-transformers==5.6.0
transformers==5.14.1
torch==2.13.0
