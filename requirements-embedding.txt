# Optional offline and online semantic-retrieval dependencies.
# This file is intentionally separate from requirements.txt because
# PyTorch and its runtime packages are substantially larger than the
# core API dependencies.

sentence-transformers==5.6.0
transformers==5.14.1
torch==2.13.0
